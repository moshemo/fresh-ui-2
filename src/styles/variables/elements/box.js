import { colors, radii, shadows, space } from "Styles"

export const box = {
  backgroundColor: colors.light,
  borderRadius: radii.curved,
  boxShadow: shadows.small,
  color: colors.dark,
  padding: space[5]
}
