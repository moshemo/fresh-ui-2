import { fontSizes } from './typography';

export const buttons = {
  sizes: {
    base: fontSizes.base,
    tiny: fontSizes.tiny,
    small: fontSizes.small,
    medium: fontSizes.h3,
    large: fontSizes.h2
  }
};
