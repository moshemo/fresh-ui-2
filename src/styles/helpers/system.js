import { css } from 'styled-components'
import {
  color,
  border,
  flexbox,
  layout,
  shadow,
  space,
  typography
} from 'styled-system'

export const baseCSS = css(
  color,
  border,
  flexbox,
  layout,
  space,
  shadow,
  typography
)
